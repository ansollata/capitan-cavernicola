# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — it contains only a placeholder `README.md` (`# capitan-cavernicola`) and has no source code, build system, dependencies, tests, or tooling configuration.

There are no commands to document (build, lint, test, run) because no language, framework, or package manifest has been chosen yet. There is no architecture to summarize because no code exists.

## When adding the first code

Before scaffolding anything substantial, confirm with the user:
- The language and framework to use
- The package manager / build tool
- Whether a particular project layout is expected

Once the project takes shape, update this file with:
- The commands actually used (build, test, lint, run a single test, dev server)
- The real architectural boundaries (entry points, module organization, key abstractions that span multiple files)
- Any conventions or constraints that aren't obvious from reading individual files
